<%= @employee.nickname %> <%= @employee.position %>

<% provide(:title, @employee.last_name) %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <%= @employee.first_name %> <%= @employee.last_name %>
        <p>
        <%= @employee.employee_email %>
        </p>
      </h1>
    </section>
  </aside>
</div>